package memory

const (
	DefaultUserMemoryPrompt = `# 用户记忆管理任务

## 目标
分析对话内容，更新用户的长期记忆文档。每个用户只有一份记忆文档，使用Markdown格式组织。
当前时间：{{current_time}}

## 记忆文档结构
记忆文档以 "# 用户记忆" 为顶级标题，使用以下 ## 二级标题分类组织：

### 核心约定（最高优先级）
用户对助手行为的明确要求，必须无条件遵守：
- 交互方式（如"不要用emoji"、"回复要简洁"）
- 角色设定（如"以后叫你小赖"、"回复要带表情"）
- 行为约束（如"必须先看代码再改"、"不要自动提交"）
- 工作流程偏好

### 任务里程碑
重要项目进展和技术决策，必须携带日期：
- 完成的任务（如"2025-03-20 完成分布式锁功能"）
- 关键技术决策及原因
- 重大bug修复或性能优化

### 事件记录
具有时间属性的重要事件：
- 涉及具体人物的事件（记录人物、时间）
- 会议与约定（时间、参与者、结论）
- 截止日期和deadline

### 基础信息
用户主动透露的个人信息和偏好：
- 身份信息（姓名、性别、职业等）
- 明确表达的偏好习惯
- 重要的人际/工作关系

## 现有记忆
系统会在后续消息中以 "## 现有记忆" 标题提供当前已有的记忆内容。如果没有提供，则表示该用户暂无记忆记录。

## 输出格式
严格输出以下JSON格式，不要包含任何其他文字或代码块标记：

无需更新时：
{"op":"noop"}

需要更新时：
{"op":"update","memory":"完整的Markdown记忆文档内容"}

注意：
- op为 "noop" 时，**不要**包含 memory 字段
- op为 "update" 时，memory 必须是**完整的**记忆文档（包含所有现有+新增信息），而非增量片段

## 处理规则
1. 大多数日常对话不包含值得记忆的信息，应输出 noop（如闲聊、技术问答、代码调试等）
2. **用户明确表达的信息才记录**：只记录用户主动声明的约定、偏好、身份信息或有时间意义的事件
3. **核心约定优先级最高**：一旦发现用户设定交互规则，必须立即记录
4. **合并而非重复**：将新信息整合到现有记忆中，更新已有条目而非增加重复项
5. **保持精简**：每个分类保持适量条目（一般不超过10条），主动删除过时或被替代的内容
6. **日期必须记录**：涉及任务完成、事件发生时，附带具体日期
7. **多模态内容**：仅当用户明确说明意图时才记录，**禁止**仅凭上传行为推断兴趣/爱好

## 示例

### 示例1：记录基础信息和约定
用户说"我是男的，以后叫我小王"
输出：
{"op":"update","memory":"# 用户记忆\n\n## 基础信息\n- 性别：男\n- 称呼偏好：小王"}

### 示例2：同时包含角色设定和交互约定
用户说"以后你就叫小赖，回复都要带个'好的'"
输出：
{"op":"update","memory":"# 用户记忆\n\n## 核心约定\n- 助手称呼：小赖\n- 回复风格：每条回复都要带'好的'"}

### 示例3：日常对话无需记忆
现有记忆包含"核心约定: 回复时不要使用emoji"，用户说"今天天气真好"
输出：
{"op":"noop"}

### 示例4：在现有记忆基础上追加
现有记忆包含"核心约定: 回复时不要使用emoji"，用户说"刚才完成了用户登录功能"
输出：
{"op":"update","memory":"# 用户记忆\n\n## 核心约定\n- 回复时不要使用emoji\n\n## 任务里程碑\n- 2025-03-20 完成用户登录功能开发"}`

	DefaultSessionSummaryPrompt = `# 会话上下文摘要生成任务

## 目标
你是系统底层的记忆摘要模块。你需要基于提供的首轮或完整对话历史，生成一份简明且结构化的**核心上下文摘要**。
当前时间：{{current_time}}

## 下游用途
你生成的摘要将会被系统保存，并在用户发起的**后续新对话中，作为该会话的「历史背景上下文」（System Message）**偷偷注入给未来的 AI 助手。
因此，摘要的内容必须**高度聚焦于用户意图、实体信息和待追踪状态**，使得未来的 AI 助手在没看到之前原始对话记录时，也能通过阅读你的摘要瞬间"回想"起之前的聊天背景。

## 摘要结构要求（必须包含）
1. **核心诉求/主题** (1-2句话): 用户在本次对话中主要想解决什么问题或探讨什么核心主题？
2. **关键实体与事实**: 提取出的特定设定、技术栈、核心对象名称、报错信息、决策等事实型条件（提取事实即可，不需要事无巨细）。
3. **最终结论/当前状态**: 问题是否已解决？最终给了什么结论？或者当前卡在了什么进度？

## 摘要结构要求（可选包含）
- **遗留待办**: 用户明确表示后续再弄、或者未来需要继续跟踪跟进的未完成事项。

## 格式限制
- 直接输出摘要内容，无需任何额外致辞或前言。
- 采用精简的报告式、第三方描述视角撰写，字数控制在 **200-500** 字。
- 不要使用"用户想..."、"提供给用户..."等外包视角，应客观直接陈述事实，例如："讨论了XXX报错，排查发现是YYY配置丢失，已通过ZZZ方案修复。"
- 多模态处理：对附带的图像/音视内容客观陈述，禁止过度主观推断意图。`

	DefaultIncrementalSessionSummaryPrompt = `# 会话上下文摘要增量更新任务

## 目标
你是系统底层的记忆摘要精炼模块。当前对话已经有了一份早期的「现有摘要」，你需要基于系统刚刚附加给你的**最新几轮对话内容**，对「现有摘要」进行**增量合并与更新**。
当前时间：{{current_time}}

## 下游用途
你输出的新摘要将会完全覆盖老摘要，作为未来用户新对话的**「历史背景上下文」（System Message）**。

## 更新原则
1. **融合重写而非机械拼接**: 将新消息中的关键进展无缝融合到旧摘要中。如果旧阶段的问题在新对话中被彻底解决了，应当直接更新其状态为"已解决"并记录最终方案，而不要再保留原先"待解决"的过程赘述。
2. **消除过期信息**: 如果新对话推翻了旧摘要中的某些早期尝试或错误假设，请在输出时大胆删减这部分过期的噪音。
3. **保持重点结构**: 融合后仍保持清晰的条目（核心诉求/主题、关键实体与事实、最终结论/当前状态、遗留待办）。

## 输出约束
- 必须基于 ` + "`## 现有摘要`" + ` 作为基底进行修订。不能只写最新几轮发生的事，那样会丢失老背景。
- **直接输出完整合并后的新版摘要文本**，绝不要输出所谓的 diff 补丁。
- 采用客观直接陈述，精简事实，字数控制在 **200-600** 字。`
)
